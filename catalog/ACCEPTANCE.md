# ACCEPTANCE.md — прогін чеклісту PHASE1_BRIEF §5–6

Сесія 2026-06-10. Команди відтворення вказані біля кожного пункту.
Контекст розбіжності контракту з ТЗ — DECISIONS.md Р-01 (демо-контракт без slab_groups/qty/площ/цін).

## §5 SEO та агентська видимість (автоматичні)

- [x] **JSON-LD кожної картки**: Product + Offer + BreadcrumbList присутні; `priceCurrency === "EUR"`; ціна JSON-LD === DOM === контракт (на демо: ціни в контракті немає → Offer без `price`, DOM «Ціна за запитом» — звірка саме цієї відповідності); availability відповідає даним (PreOrder, Р-06); draft/hidden відсутні. → `npm run build` (scripts/check-public-artifacts.mjs, крок 5): **4/4 карток PASS**
- [x] **`/catalog.json`** валідний проти `catalog.public.schema.json`; grep: жодного `qty_slabs`, `notes_internal`, `todo_manual`, не-published id. → check-public-artifacts кроки 1–3: **PASS**
- [x] **`/llms.txt`** існує і посилається на живий фід. → check-public-artifacts крок 4 + Playwright «/llms.txt живий»: **PASS**
- [x] **Сторінки рендеряться без JS (SSG)** — HTML містить назву, ціну, характеристики. → Playwright «сторінка рендериться без JS» (request без браузера): **PASS**

## §6 Автоматичні (CI-able)

- [x] **`npm run validate`** — схема + integrity зелені. Перевірка €/slab ≈ €/м² × площа реалізована, на демо-даних чесний **SKIP** (немає slab_groups із цінами/площами — Р-04); решта integrity (унікальність/slug id, посилання line→lines) активні: **PASS**
- [x] **Білд без errors/warnings власних скриптів** — validate + astro build + artifact-check чисті; `astro check`: 0 errors / 0 warnings / 0 hints. Warnings сторонніх інструментів зафіксовані в DECISIONS.md з оцінкою (не блокери): **PASS**
- [x] **`npm run test:negative-data`** — працює з тимчасовими копіями; білд падає на: відсутньому обовʼязковому полі, битому посиланні line, розʼїзді цін >5%; реальний JSON byte-for-byte недоторканий: **PASS**
- [x] **Playwright: число карток із даних** — рівно `published` (4) у production; SHOW_DRAFTS=1 у dev дає 5 із бейджем «Чернетка», production build із SHOW_DRAFTS=1 — далі 4, без витоку id: **PASS**
- [x] **Кожен фільтр звужує; комбінація = перетин; скинути повертає все** — Playwright проганяє кожну опцію кожного фасета проти очікування, порахованого з контракту: **PASS**
- [x] **Marketplace-ready** — `npm run test:marketplace`: копія контракту + матеріал нової лінії → повний білд → нова опція фільтра, нова картка, нова сторінка без зміни коду; grep: доступ до altaco_materials лише в `src/lib/`: **PASS**
- [x] **`/katalog/{id}` відкривається; 404 для не-published і неіснуючих** — Playwright (mystic-grey → 404, no-such-material → 404): **PASS**
- [x] **Ціни через єдину функцію форматування** — `src/lib/format.ts`; Playwright порівнює очікуваний рядок з DOM. На демо-даних (усі ціни 0-плейсхолдери) очікуваний рядок — «Ціна за запитом» у сітці й на картці (Р-03); гілки «від €min»/фактичних цін реалізовані та активуються реальними цінами: **PASS (з застереженням Р-03)**
- [x] **Grep по dist/** (21 файл, включно з JS/sourcemaps): жодного `qty_slabs`, `notes_internal`, `todo_manual`: **PASS**
- [x] **Lighthouse (mobile)**: головна 99/100/100/100, картка 99/100/100/100 (Perf/A11y/BP/SEO) — пороги ≥90 перевищено: **PASS**
- [x] **title/meta/OG валідні; sitemap.xml; без кирилиці в URL** — Playwright + artifact-check (sitemap: 5 сторінок, латиниця, лише published): **PASS**

## §6 Ручні (Олександр, ~15 хв) — до перевірки людиною

- [ ] Візуальна відповідність макетам презентації (сітка, картка) — скриншоти сесії в /tmp надіслані; самооцінка: структура макетів дотримана, фото — генеровані плейсхолдери до появи реальних (Р-07)
- [ ] Тексти без AI-маркерів, термінологія прайсу збережена
- [ ] Мобільний вигляд на реальному телефоні
- [ ] Вибіркова перевірка JSON-LD через Rich Results Test (потрібен публічний URL після деплою)

## Чесні N/A на демо-контракті (оживають із багатим контрактом, код готовий)

- Інтегріті €/slab ≈ €/м²×площа — SKIP: немає полів (перевірка реалізована, негативний тест доводить, що вона ловить розʼїзд).
- «від €min» з реальним числом — усі ціни 0 → «Ціна за запитом».
- Фільтри колір/товщина/наявність — полів немає в контракті (Р-05).
- Варіанти slab_groups на картці — груп немає в контракті.
- Бейдж наявності — тепер із поля `stock_location` (kyiv/dnipro/on_order), Р-24.
