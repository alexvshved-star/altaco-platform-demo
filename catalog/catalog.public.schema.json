{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://altaco.example/schema/catalog.public.schema.json",
  "title": "ALTACO public catalog feed (/catalog.json)",
  "description": "Схема публічної проєкції контракту. Тільки published-позиції, БЕЗ внутрішніх полів (qty_slabs, notes_internal, todo_manual, $comment*, availability_overlay). Цей фід їсть консультант Фази 2 і зовнішні агенти (GPTBot/ClaudeBot/PerplexityBot).",
  "type": "object",
  "required": ["version", "generated_at", "business", "lines", "materials"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "string" },
    "generated_at": { "type": "string", "format": "date-time" },
    "business": {
      "type": "object",
      "required": ["name", "url"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "url": { "type": "string", "format": "uri" },
        "address": { "type": "string" },
        "city": { "type": "string" }
      }
    },
    "lines": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    },
    "materials": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "slug", "type", "line", "brand", "page_url", "currency", "availability", "availability_status", "price_m2", "price_slab"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
          "name": { "type": "string" },
          "slug": { "type": "string", "pattern": "^[a-z0-9-]+$" },
          "type": { "type": "string" },
          "type_label": { "type": "string" },
          "line": { "type": "string" },
          "line_name": { "type": "string" },
          "brand": { "type": "string" },
          "color": { "type": "string" },
          "finish": { "type": "string" },
          "finish_label": { "type": "string" },
          "thickness_mm": { "type": "number", "minimum": 0 },
          "size": { "type": "string" },
          "area_m2": { "type": "number", "minimum": 0 },
          "price_m2": { "type": ["number", "null"], "minimum": 0 },
          "price_slab": { "type": ["number", "null"], "minimum": 0 },
          "currency": { "type": "string", "enum": ["EUR"] },
          "availability": { "type": "string", "enum": ["InStock", "PreOrder"] },
          "availability_status": { "type": "string" },
          "city": { "type": "string" },
          "image_url": { "type": "string", "format": "uri" },
          "page_url": { "type": "string", "format": "uri" },
          "applications": { "type": "array", "items": { "type": "string" } },
          "short_description": { "type": "string" }
        }
      }
    }
  }
}
